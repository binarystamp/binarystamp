{
  "name": "binarystamp-subgraph",
  "version": "1.0.0",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy binarystamp/binarystamp --node https://api.studio.thegraph.com/deploy/",
    "create-local": "graph create binarystamp/binarystamp --node http://localhost:8020/",
    "deploy-local": "graph deploy binarystamp/binarystamp --node http://localhost:8020/ --ipfs http://localhost:5001/"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "^0.98.0",
    "@graphprotocol/graph-ts": "^0.35.0"
  }
}
