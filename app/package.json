{
  "name": "binarystamp-frontend",
  "private": true,
  "description": "Browser-side Sui SDK bundle for BinaryStamp",
  "scripts": {
    "build": "esbuild scripts/sui-entry.js --bundle --format=esm --minify --target=es2020 --outfile=frontend/vendor/sui.js"
  },
  "dependencies": {
    "@mysten/sui": "2.22.1"
  },
  "devDependencies": {
    "esbuild": "0.28.1",
    "jsdom": "^29.1.1"
  }
}
